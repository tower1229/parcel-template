{
  "name": "parcel-template",
  "version": "0.0.1",
  "description": "An parcel template project",
  "main": "dist/index.js",
  "scripts": {
    "dev": "rm -rf .cache && rm -rf build && parcel src/index.html -d build",
    "build": "rm -rf .cache && rm -rf dist && parcel build src/index.html"
  },
  "author": "tower1229@foxmail.com",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.2.3",
    "autoprefixer": "^9.4.4",
    "parcel-bundler": "^1.11.0",
    "postcss-modules": "^1.4.1"
  },
  "license": "MIT",
  "browserslist": [
    "last 1 version",
    "> 1%",
    "IE 10"
  ]
}
